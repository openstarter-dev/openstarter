# web 应用（同时也是 API）的源。插件由此派生 host_permissions 与 API base URL
# 留空时：wxt.config.ts 在构建期自动从 monorepo 根 .env 的 OPENSTARTER_API_URL 派生
# （与 web/cli/desktop/mobile 同源）。本处的 VITE_APP_URL 优先级高于根值，CI/特殊场景可在此覆盖。
VITE_APP_URL=http://localhost:3000
