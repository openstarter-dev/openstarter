import { Text } from "react-native";

import { Screen } from "@/components/ui/screen";

export default function SignInScreen() {
  return (
    <Screen>
      <Text className="text-foreground dark:text-dark-foreground">Sign in</Text>
    </Screen>
  );
}
